import { LinearProgress } from '@material-ui/core';
import styled from 'styled-components';

export const DivButton = styled.div`
  display: inline-block;
`;

export const LinearProgressBordeRadius = styled(LinearProgress)`
  border-radius: 15px;
  margin-left: 5px;
  margin-right: 5px;
`;
